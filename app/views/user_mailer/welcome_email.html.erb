<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
        <div class = "container">

        <h2>Welcome to Know Who !Followed</h2>
        <% if not  @ids.blank? %> 
        		<h1>You have <%= @ids.count %> Unfollowers Since Last Visit </h1>
        		<% i=0 %>
            <table class = "table table-striped">
        		<% @ids.each do |i| %>
        			<tr><td><%= @hash[i] %></td></tr>
        		<% end %>
            </table>
          <% else %>
            <h1>Congratulations !! No Unfollowers</h1>
      	<% end %>

         <% if not  @new_followers.blank? %> 
            <h1>You have <%= @new_followers.count %> New followers Since Last Visit</h1>
            <% i=0 %>
            <table class = "table table-striped">
            <% @new_followers.each do |i| %>
              <tr><td><%= @new_hash[i] %></td></tr>
            <% end %>
            </table>
        <% end %>
      </div>
 
</html>